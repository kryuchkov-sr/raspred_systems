# Лабораторная работа № 2. Проектирование и реализация клиент-серверной системы. HTTP, веб-серверы и RESTful веб-сервисы

### Цель работы ### 
Изучить методы отправки и анализа HTTP-запросов сиспользованием инструментов telnet и curl, освоить базовую настройку и анализ работы HTTP-сервера nginx в качестве веб-сервера и обратного
прокси, а также изучить и применить на практике концепции архитектурного стиля REST для создания веб-сервисов (API) на языке Python.

---

### Теоретические основы ###
1. **HTTP (HyperText Transfer Protocol):** протокол прикладного уровня для передачи данных, где клиент (например, браузер) отправляет HTTP запрос, а сервер отвечает HTTP-ответом.
2. **Telnet и Curl:**
 - telnet - утилита для взаимодействия с удаленными хостами по заданному порту. Позволяет "сырыми" байтами отправлять HTTP запросы и видеть "сырой" ответ сервера, что полезно для понимания структуры протокола.
 - Curl - инструмент для отправки запросов по различным протоколам, включая HTTP/HTTPS. Автоматически обрабатывает редиректы, HTTPS, позволяет легко управлять заголовками, методами и телом запроса.
3. **Nginx:** высокопроизводительный веб-сервер, выполнятющий две ключевые роли в данной работе:
- веб-сервер - напрямую отдает статические файлы (HTML, CSS, картинки).
- обратный прокси (Reverse Proxy) - принимает запросы от клиентов и перенаправляет их на другие серверы (в нашем случае — на Python-приложение).
4.  **REST (Representational State Transfer)** — это архитектурный стиль для создания распределенных систем. API, построенный с учетом принципов REST, называется RESTful.

---

### Вариант 14 ###

| №  | HTTP-анализ(curl/telnet)                                   | REST API "Бизнес-логика"                                                              | Настройка Nginx                                               |
|----|-----------------------------------------------|--------------------------------------------------------------------------|--------------------------------------------------------|
| 14 | Получение данных о рейсах с сайта aeroflot.ru (анализ HTML или JS-запросов). | API для "Ресторанное меню" (сущность: id, dish_name, price, category). | Настроить ограничение (rate limit) в 5 запросов за 10 секунд с одного IP. |

---
## Задание 1. Получение данных о рейсах с сайта aeroflot.ru (анализ HTML или JS-запросов).
---
```bash
### HTML-вывод главной страницы
curl -v https://uralairlines.ru
```
![html](https://github.com/user-attachments/assets/6cc566f6-e87f-4379-a1a6-e61daf63baa7)


## Задание 2 🍽️ Ресторанное API с кешированием и ограничением запросов RESTful API для управления меню ресторана, развёрнутое за reverse proxy (Nginx) с поддержкой кеширования и rate limiting.

---

### 📦 Установка и подготовка

#### 1. Виртуальное окружение
```bash
### Установка venv (если ещё не установлено)
sudo apt install python3-venv -y

### Создание и активация окружения
python3 -m venv venv
source venv/bin/activate
```

```bash
### Установка Flask
pip install Flask

### Установка вспомогательных утилит
sudo apt update
sudo apt install telnet curl jq nginx -y
```


### 2. Запуск Flask-приложения. 
В терминале с активным (venv) запускается сервер:
```bash
python3 app.py
```
![запуск flask](https://github.com/user-attachments/assets/6931afd4-087f-4f59-a45a-bc1d59017a8b)

```bash
### Установка jq
sudo apt install jq -y
```
**Получение всех блюд:**
```bash
curl -s http://127.0.0.1:5000/api/dishes | jq
```
![блюда](https://github.com/user-attachments/assets/1af82ea3-e54e-4fd3-af51-c3c1ce29b181)

**Получение финансовой сводки по меню:**
```bash
curl -s http://127.0.0.1:5000/api/dishes/menu_summary | jq
```
![блюда сумма](https://github.com/user-attachments/assets/2b5dbb0c-1b1f-455c-aac4-5870a6610460)


**Создание нового блюда:**
```bash
curl -X POST http://127.0.0.1:5000/api/dishes -H "Content-Type: application/json" -d '{"dish_name": "Борщ", "price": 300, "category": "Супы"}' | jq
```
![борщ](https://github.com/user-attachments/assets/fe221fa3-91d8-4b71-86a0-fbc22d339729)

**Финальная проверка:**
```bash
curl http://127.0.0.1:5000/api/dishes | jq
```
![все блюда](https://github.com/user-attachments/assets/93731e6f-6ec1-4c03-ad3c-32ab1d42c78a)

---
## Задание 3. Настроить ограничение (rate limit) в 5 запросов за 10 секунд с одного IP.
---

### 1. Подготовка Nginx.
```bash
Установка Nginx.
sudo apt install -y nginx

Запуск и добавление в автозагрузку.
sudo systemctl start nginx
sudo systemctl enable nginx

Проверка статус.
sudo systemctl status nginx
```
![nginx активация](https://github.com/user-attachments/assets/66cf10e8-dd59-4e99-8630-a3b96d2d128a)

---
### 2. Настройка Nginx.
```bash
### Место и правила хранения ответов
sudo nano /etc/nginx/nginx.conf
```
В большом блоке **http { ... }**, нужно добавить одну строку, которая определяет "зону кеширования":<br/> 
`proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=api_cache:10m inactive=60m;` 
![конф1](https://github.com/user-attachments/assets/d91ed98d-7967-41b1-8cf4-80289946e854)

Требуется объяснить Nginx, как обрабатывать запросы, которые приходят на адрес, начинающийся с /api/.
```bash
sudo nano /etc/nginx/sites-available/default
```
![конф2(обн)](https://github.com/user-attachments/assets/11fa496a-5077-48a3-831d-bbd947fe2f9b)

Настройки изменены, но Nginx все еще работает по-старому. Нужно заставить его 
перечитать файлы конфигурации.
```bash
### Проверяем, нет ли в наших файлах синтаксических ошибок
sudo nginx-t
```
![успешно](https://github.com/user-attachments/assets/69770962-e45f-4729-9988-6a1aec4fe3a3)

Перезапускаем Nginx, чтобы он применил изменения
```bash
sudo systemctl restart nginx
```
---
### 3. Тестирование Nginx.

Подготовка к тестированию
1.Терминал 1 (Кухня). Убедитесь, что в одном терминале у вас запущено Flask
приложение. Вы должны видеть строки вроде * Running on http://127.0.0.1:5000. Не 
закрывайте его.
2.Терминал 2 (Клиент). Откройте второй, чистый терминал. Все команды ниже мы 
будем выполнять именно в нем.
3.Nginx. Он должен работать в фоне после команды sudo systemctl restart nginx.
![ng 3 терминала](https://github.com/user-attachments/assets/8bd0aee2-c48a-4763-b709-c6a942c02fc5)

#### Тест 1. Работает ли Nginx сам по себе?
Будем отправлять все запросы на http://localhost, то есть на порт 80, где нас встречает Nginx ("официант"). <br/>
```bash
curl http://localhost
```
![n1](https://github.com/user-attachments/assets/63157d3e-10f6-4ec9-b340-67bedc41a2a5)

#### Тест 2. Работает ли проксирование и кеширование? (Самый главный тест)
Теперь проверим, что запросы к /api/dishes правильно перенаправляются на Flask и кешируются.
![n2](https://github.com/user-attachments/assets/880c74d8-3171-486a-86bb-6386c7f6178e)

#### Тест 3. Работают ли запросы, которые не должны кешироваться?
Теперь добавим новую транзакцию. POST-запросы изменяют данные, поэтому Nginx по умолчанию не должен их кешировать.
```bash
curl -X POST http://127.0.0.1:5000/api/dishes \ -H "Content-Type: application/json" \ -d '{"dish_name": "Стейк Рибай", "price": 1200, "category": "Основные блюда"}'
```
![n3](https://github.com/user-attachments/assets/8231aca4-3991-478b-89b1-36152f60a042)

Теперь добавим новую транзакцию. POST-запросы изменяют данные, поэтому Nginx по умолчанию не должен их кешировать.
```bash
curl http://127.0.0.1:5000/api/dishes |jq
```
![n3 1](https://github.com/user-attachments/assets/daed51fa-a10c-4323-b6d3-c9e861f04a77)

![limit](https://github.com/user-attachments/assets/fab14343-89ee-4a7f-9852-8b3d4d0d0904)
---
### Выоводы ###
В ходе работы была проанализирована работа сайта aeroflot.ru, отправляя HTTP-запросы через curl и telnet для изучения структуры протокола.
Разработан REST API на Python для управления меню ресторана (операции с блюдами).Был настроен Nginx как обратный прокси для Python-приложения и реализован rate limiting (5 запросов/10 сек с одного IP).

Полученные навыки:
HTTP/Сетевое взаимодействие: Глубокое понимание HTTP через прямой анализ запросов/ответов.
Администрирование: Настройка Nginx в ролях прокси-сервера и средства защиты (ограничение запросов).
Комплексное решение: Умение связывать разные компоненты стека (клиент — API — веб-сервер) в работающую систему.

